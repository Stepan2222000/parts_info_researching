-- 013: вложенные наборы (kit-in-kit).
-- Компонент набора может сам быть набором: research-агент помечает это флагом
-- is_kit на компоненте (состав под-набора в том же ране не раскрывается — только
-- его номер; глубина собирается отдельными ранами под-наборов).
-- NULL = не выяснено (и все строки, записанные до этой миграции).
--
-- В smart НИЧЕГО не меняется: вложенность там выражается цепочками
-- part_components (parent_id -> child_id), таблица уже это поддерживает.

ALTER TABLE draft_kit_components ADD COLUMN is_kit BOOLEAN;
